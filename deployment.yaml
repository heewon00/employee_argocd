piVersion: apps/v1
kind: Deployment
metadata:
  name: employee-frontend
  labels:
    app: employee-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: employee-frontend
  template:
    metadata:
      labels:
        app: employee-frontend
    spec:
      containers:
      - name: employee-frontend
        imagePullPolicy: Always
        image: ghcr.io/heewon00/vue_crud_security:test_v5
          env:
          - name: BACKEND_API_URL
           value: "http:/employee-backend" 
        ports:
        - containerPort: 80
